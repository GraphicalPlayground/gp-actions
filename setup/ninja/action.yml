name: 'Setup Ninja'
description: 'Set up Ninja and cache the necessary files for faster builds.'
branding:
  icon: 'zap'
  color: 'gray-dark'
inputs:
  use-cache:
    description: 'Whether to use caching for Ninja files.'
    required: false
    default: 'true'
runs:
  using: 'composite'
  steps:
    - name: 'Set up Ninja'
      shell: bash
      run: |
        echo "Setting up Ninja..."
        # Add commands to set up Ninja here

    - name: 'Cache Ninja files'
      shell: bash
      if: ${{ inputs.use-cache == 'true' }}
      run: |
        echo "Caching Ninja files..."
        # Add commands to cache Ninja files here
