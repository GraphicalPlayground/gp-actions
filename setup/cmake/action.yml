name: 'Setup CMake'
description: 'Set up CMake and cache the necessary files for faster builds.'
branding:
  icon: 'settings'
  color: 'green'
inputs:
  use-cache:
    description: 'Whether to use caching for CMake files.'
    required: false
    default: 'true'
runs:
  using: 'composite'
  steps:
    - name: 'Set up CMake'
      shell: bash
      run: |
        echo "Setting up CMake..."
        # Add commands to set up CMake here

    - name: 'Cache CMake files'
      shell: bash
      if: ${{ inputs.use-cache == 'true' }}
      run: |
        echo "Caching CMake files..."
        # Add commands to cache CMake files here
