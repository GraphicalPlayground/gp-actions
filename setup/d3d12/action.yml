name: 'Setup DirectX 12'
description: 'Set up DirectX 12 and cache the necessary files for faster builds.'
branding:
  icon: 'box'
  color: 'green'
inputs:
  use-cache:
    description: 'Whether to use caching for DirectX 12 files.'
    required: false
    default: 'true'
runs:
  using: 'composite'
  steps:
    - name: 'Set up DirectX 12'
      shell: bash
      run: |
        echo "Setting up DirectX 12..."
        # Add commands to set up DirectX 12 here

    - name: 'Cache DirectX 12 files'
      shell: bash
      if: ${{ inputs.use-cache == 'true' }}
      run: |
        echo "Caching DirectX 12 files..."
        # Add commands to cache DirectX 12 files here
