name: 'Setup Metal'
description: 'Set up Metal and cache the necessary files for faster builds.'
branding:
  icon: 'cpu'
  color: 'gray-dark'
inputs:
  use-cache:
    description: 'Whether to use caching for Metal files.'
    required: false
    default: 'true'
runs:
  using: 'composite'
  steps:
    - name: 'Set up Metal'
      shell: bash
      run: |
        echo "Setting up Metal..."
        # Add commands to set up Metal here

    - name: 'Cache Metal files'
      shell: bash
      if: ${{ inputs.use-cache == 'true' }}
      run: |
        echo "Caching Metal files..."
        # Add commands to cache Metal files here
