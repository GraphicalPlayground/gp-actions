name: 'Setup SDL3'
description: 'Set up SDL3 and cache the necessary files for faster builds.'
branding:
  icon: 'layers'
  color: 'purple'
inputs:
  use-cache:
    description: 'Whether to use caching for SDL3 files.'
    required: false
    default: 'true'
runs:
  using: 'composite'
  steps:
    - name: 'Set up SDL3'
      shell: bash
      run: |
        echo "Setting up SDL3..."
        # Add commands to set up SDL3 here

    - name: 'Cache SDL3 files'
      shell: bash
      if: ${{ inputs.use-cache == 'true' }}
      run: |
        echo "Caching SDL3 files..."
        # Add commands to cache SDL3 files here
